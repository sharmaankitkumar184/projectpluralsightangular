import { ConvertTospacesPipe } from './convert-tospaces.pipe';

describe('ConvertTospacesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTospacesPipe();
    expect(pipe).toBeTruthy();
  });
});
